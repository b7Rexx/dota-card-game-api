language: node_js
node_js:
  - 12
branches:
  only:
    - master

env: >
  NODE_ENV=production
  APP_NAME='Dota Card Game Api'
  APP_VERSION='1.0.0'
  APP_PORT='8080'
  APP_HOST='localhost'
  APP_URL='http://localhost:8080'

  DB_PORT='3306'
  DB_HOST='remotemysql.com'
  DB_NAME='8fboJ1HdkT'
  DB_USER='8fboJ1HdkT'
  DB_PASSWORD='k1XNr1pcb9'

before_script:
  - cp .env.example .env
script:
  - npm install
